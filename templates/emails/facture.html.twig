<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Merci de votre achat !</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h2 class="mt-4">Facture n°{{commande.id}}</h2>

        <p>Bonjour {{user.prenom}}, Vous trouverez une copie des détails de votre transaction ci-dessous, merci d'avoir commandé sur Nkinel !</p>
        
        <table class="table table-bordered bg-white">
            <thead>
                <tr>
                    <th>Article</th>
                    <th>Quantité</th>
                    <th>Prix unitaire</th>
                    <th>Sous-total</th>
                </tr>
            </thead>
            <tbody>
                {% for articleData in articles %}
                    <tr>
                        <td>{{ articleData.article.nom }}</td>
                        <td>{{ articleData.quantite }}</td>
                        <td>{{ articleData.prix|number_format(2, '.', ',') }} €</td>
                        <td>{{ articleData.sousTotal|number_format(2, '.', ',') }} €</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        
        <p class="mt-4">Total de la commande : {{ prixTotal|number_format(2, '.', ',')}} €</p>
    </div>
</body>
</html>
