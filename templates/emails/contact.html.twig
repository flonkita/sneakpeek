<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>Nouveau mail de contact</title>
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
	</head>
	<body>
		<div class="container mt-5">
			<div class="row">
				<div class="col-md-6 offset-md-3">
					<div class="text-center">
						<img src="{{ absolute_url(asset('img/logo.png')) }}" width="128" alt="">
					</div>
					<h2 class="text-center">Nouveau mail de contact</h2>
					<p>Vous avez reçu un nouveau message de contact :</p>
					<ul class="list-group">
						<li class="list-group-item">
							<strong>Nom :</strong>
							{{ contact.name }}</li>
						<li class="list-group-item">
							<strong>Email :</strong>
							{{ contact.email }}</li>
					</ul>
					<div class="mt-4">
						<p>
							<strong>Message :</strong>
						</p>
						<p>{{ contact.message }}</p>
					</p>
				</div>
			</div>
		</div>
	</body>
</html></div></body></html>
